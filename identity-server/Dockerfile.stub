FROM python:3.13-slim
WORKDIR /app
COPY pyproject.toml .
RUN pip install uv && uv pip install --system -e ".[server]"
COPY identity_server ./identity_server
COPY domain_stub ./domain_stub
CMD ["uvicorn", "domain_stub.app:app", "--host", "0.0.0.0", "--port", "8080"]
